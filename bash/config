#PS1="\w$ "

# Set a custom prompt
[[ -s "$HOME/.bash/ps1_functions.sh" ]] &&
      source ~/.bash/ps1_functions.sh
ps1_set --prompt ∴

# Set editor to TextWrangler
#
# Set editor to Ox (brew install ox)
export EDITOR="ox"

# Allow GPG to sign keys
export GPG_TTY=$(tty)