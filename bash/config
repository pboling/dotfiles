#PS1="\w$ "
#PS1="\$(~/.rvm/bin/rvm-prompt) $PS1"

# RVM must be installed for this to work.
[[ -s "$HOME/.rvm/contrib/ps1_functions" ]] &&
      source "$HOME/.rvm/contrib/ps1_functions"
ps1_set --prompt ∴

# Set editor to TextWrangler
export EDITOR="nano"
# Set editor to bin/mate_wait script
#export EDITOR="mate_wait"
